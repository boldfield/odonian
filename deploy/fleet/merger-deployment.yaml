apiVersion: apps/v1
kind: Deployment
metadata:
  name: merger
  namespace: odonian-fleet
  labels:
    app.kubernetes.io/name: odonian-fleet
    app.kubernetes.io/component: merger
spec:
  # Mergers are cheap and non-LLM, so the arm64 Pi cluster is the ideal home —
  # apply this manifest to the `summercamp-lab` (Pi) cluster.
  replicas: 4
  selector:
    matchLabels:
      app.kubernetes.io/component: merger
  template:
    metadata:
      labels:
        app.kubernetes.io/name: odonian-fleet
        app.kubernetes.io/component: merger
    spec:
      # Run as a numeric non-root uid (the image's `agent` user = 1000). `runAsNonRoot` alone fails
      # against an image whose USER is a NAME — the kubelet can't verify a name is non-root. `fsGroup`
      # makes the emptyDir HOME writable by the group.
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      # Prefer arm64 (Pis) but allow amd64 as overflow. (Within a single-arch cluster this is a no-op;
      # it only matters if a cluster is mixed-arch.)
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - { key: kubernetes.io/arch, operator: In, values: ["arm64"] }
      containers:
        - name: merger
          # Version tag is bumped by hand on release; make merger-image must have pushed this tag first.
          image: docker.summercamp.eastharbor.casa:32050/odonian/merger:v0.16.0-17-gbd57579
          args: ["--kind", "merge"]
          env:
            # All fleet pods reach the server via its PUBLIC ingress, so placement is decoupled from
            # the server's cluster (lab pods can't use cp's cluster-DNS service name).
            - name: ODONIAN_URL
              value: "https://odonian.summercamp.eastharbor.casa"
            - name: ODONIAN_PROJECT
              value: "all" # multi-project: drain merge tasks across every board
            - name: HOME
              value: /home/agent
            - name: ODONIAN_HOME
              value: /home/agent/.odonian
            - name: ODONIAN_TOKEN
              valueFrom:
                secretKeyRef:
                  name: odonian-fleet
                  key: token
          volumeMounts:
            - { name: home, mountPath: /home/agent }
            - { name: forge-tokens, mountPath: /etc/fleet, readOnly: true }
          resources:
            requests: { cpu: "50m", memory: "64Mi" }
            limits: { cpu: "250m", memory: "128Mi" }
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities: { drop: ["ALL"] }
      volumes:
        - { name: home, emptyDir: {} }
        - name: forge-tokens
          secret:
            secretName: odonian-forge-tokens
